{
    "version": 2,
    "builds": [
      {
        "src": "server.js", 
        "use": "prisma generate --no-engine && @vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server.js"
      }
    ]
  }